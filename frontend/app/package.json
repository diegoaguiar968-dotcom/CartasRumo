{
  "name": "agente-rumo-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'static build'",
    "start": "serve -s dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
